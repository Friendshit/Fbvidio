import random
import sys
import time
def memek(s):
    for c in s + '\n':
        sys.stdout.write(c)
        sys.stdout.flush()
        time.sleep(random.random() * 1)
memek('Sedang melakukan donload ...')
